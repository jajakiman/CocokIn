# Issue Tracker

Issues live in GitHub Issues for `jajakiman/CocokIn`.

- Use one issue per independently testable vertical slice.
- Pull Requests are not automatically treated as new triage requests.
- Link each Pull Request to its issue and include verification evidence.
- Use blocking links or an explicit `Blocked by` section for dependencies.
